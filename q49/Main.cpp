#include <bits/stdc++.h>
using namespace std;

int main() {
    int N, M;
    cin >> N >> M;

    vector<long long> C(M), L(M), R(M);

    for (int i = 0; i < M; i++) {
        cin >> C[i] >> L[i] >> R[i];
    }

    return 0;
}